; =============================================================================
; Supervisor Configuration for KYC-AML Agentic AI Orchestrator
; =============================================================================
; Install: sudo bash deploy/install-supervisor.sh
; 
; Commands:
;   supervisorctl status kyc-aml-orchestrator
;   supervisorctl start kyc-aml-orchestrator
;   supervisorctl stop kyc-aml-orchestrator
;   supervisorctl restart kyc-aml-orchestrator
;   supervisorctl tail -f kyc-aml-orchestrator
; =============================================================================

[program:kyc-aml-orchestrator]
; Application command - runs Streamlit
command=/home/g2023aa05111/kyc-aml-agentic-ai-orchestrator/.venv/bin/streamlit run web_chat.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true --browser.gatherUsageStats=false

; Working directory
directory=/home/g2023aa05111/kyc-aml-agentic-ai-orchestrator

; Run as application user
user=g2023aa05111

; Auto-start and restart settings
autostart=true
autorestart=true
startsecs=10
startretries=3

; Stop settings
stopwaitsecs=30
stopasgroup=true
killasgroup=true

; Logging - store in application's logs directory
stdout_logfile=/home/g2023aa05111/kyc-aml-agentic-ai-orchestrator/logs/supervisor_app.log
stdout_logfile_maxbytes=50MB
stdout_logfile_backups=5
stderr_logfile=/home/g2023aa05111/kyc-aml-agentic-ai-orchestrator/logs/supervisor_error.log
stderr_logfile_maxbytes=10MB
stderr_logfile_backups=3
redirect_stderr=false

; Environment variables (API keys should be in .env file, not here)
environment=
    HOME="/home/g2023aa05111",
    PATH="/home/g2023aa05111/kyc-aml-agentic-ai-orchestrator/.venv/bin:/usr/local/bin:/usr/bin:/bin",
    PYTHONUNBUFFERED="1"

; Process priority (lower = higher priority)
priority=999

; Number of processes (1 for Streamlit)
numprocs=1
