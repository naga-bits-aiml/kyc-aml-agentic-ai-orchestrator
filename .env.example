# =============================================================================
# KYC-AML AGENTIC AI ORCHESTRATOR - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and update with your actual values
# These variables are referenced in config.json using ${VAR_NAME} syntax
# =============================================================================

# -----------------------------------------------------------------------------
# OPENAI CONFIGURATION
# -----------------------------------------------------------------------------
# Required for LLM-based agent orchestration
OPENAI_API_KEY=your_openai_api_key_here


# -----------------------------------------------------------------------------
# DOCUMENT CLASSIFIER API
# -----------------------------------------------------------------------------
# External API for document classification
CLASSIFIER_API_KEY=your_classifier_api_key_here


# -----------------------------------------------------------------------------
# ALTERNATIVE LLM PROVIDERS (Optional)
# -----------------------------------------------------------------------------
# Only needed if switching from OpenAI in config.json

# Azure OpenAI
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4-turbo

# Anthropic (Claude)
ANTHROPIC_API_KEY=your_anthropic_api_key_here


# =============================================================================
# CONFIGURATION NOTES
# =============================================================================
# 
# 1. MAIN CONFIGURATION:
#    - Edit config.json for all application settings
#    - This .env file only contains sensitive API keys
#    - Environment variables are resolved in config.json using ${VAR_NAME}
#
# 2. REQUIRED VARIABLES:
#    - OPENAI_API_KEY: Required for agent orchestration
#    - CLASSIFIER_API_KEY: Optional, for external classifier API
#
# 3. SECURITY:
#    - Never commit this file with real API keys
#    - Use different .env files for different environments
#    - The .env file is already in .gitignore
#
# 4. CHANGING SETTINGS:
#    - Paths, timeouts, models: Edit config.json
#    - API keys only: Edit this .env file
#    - No code changes needed for configuration updates
#
# =============================================================================
